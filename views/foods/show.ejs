<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Pantry Item</title>
</head>
<body>
  <%- include('../partials/_navbar.ejs') %>
  <h1>Pantry Item</h1>

  <p><strong>Name:</strong> <%= item.name %></p>

  <p>
    <a href="/users/<%= userId %>/foods/<%= item._id %>/edit">Edit</a>
    <form action="/users/<%= userId %>/foods/<%= item._id %>?_method=DELETE" method="POST" style="display:inline">
      <button type="submit">Delete</button>
    </form>
  </p>

  <p><a href="/users/<%= userId %>/foods">Back to Pantry</a></p>
</body>
</html>
